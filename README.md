# Relationship AI MVP — Free-tier Scaffold

Minimal privacy-first Relationship AI MVP (FastAPI backend + React frontend).
Uses client-side encryption for journal text (recommended). This scaffold is configured for free hosting:
- Backend: Render (free tier) or local
- Frontend: Vercel (free tier) or local
- AI: Gemini 2.0 Flash (provide your key as an environment variable when deploying)

IMPORTANT: Do **not** commit your real API keys to GitHub. Use environment variables on Render/Vercel.

See `deploy-instructions.md` for step-by-step deploy notes.
